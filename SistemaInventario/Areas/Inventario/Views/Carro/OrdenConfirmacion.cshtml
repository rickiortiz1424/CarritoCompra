<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Factura</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        .container {
            width: 80%;
            margin: 20px auto;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }

        .header {
            text-align: center;
            padding-bottom: 20px;
        }

            .header img {
                width: 50%;
            }

        .thank-you {
            text-align: center;
            color: maroon;
            margin-top: 20px;
        }

        .order-details {
            margin-top: 20px;
            text-align: center;
        }

        .invoice-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

            .invoice-table th, .invoice-table td {
                border: 1px solid #ddd;
                padding: 10px;
                text-align: center;
            }

        .exit-button {
            text-align: center;
            margin-top: 20px;
        }

        .btn {
            display: inline-block;
            padding: 10px 20px;
            font-size: 16px;
            text-align: center;
            text-decoration: none;
            background-color: #28a745;
            color: #fff;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="header">
            <h1 class="text-primary">Orden Procesada Correctamente</h1>

        </div>

        <div class="thank-you">
            <p>Gracias Por Agendar esta cita!</p>
            <p>Has agendado tu cita y nosotros te enviaremos un email pronto!</p>
            <p>Tu numero de Orden es: <span>@Model</span></p>
        </div>

        <div class="order-details">
            <table class="invoice-table">
                <thead>
                    <tr>
                        <th>Producto</th>
                        <th>Cantidad</th>
                        <th>Precio Unitario</th>
                        <th>Total</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Aquí puedes agregar filas para cada producto o servicio -->
                    <tr>
                        <td>Vehiculo</td>
                        <td>1</td>
                        <td>$1000.00</td>
                        <td>$1000.00</td>
                    </tr>
                 
                    <!-- Puedes agregar más filas según sea necesario -->
                </tbody>
            </table>
        </div>

       
    <a asp-area="Inventario" asp-controller="Home" asp-action="Index" class="btn btn-success">Salir</a>
    </div>

</body>
</html>



@section Scripts {

    <script>
     swal({
        title: "Gracias por agendar esta cita",
        text: "Su Cita se ah agendado Correctamente",
        icon: "success",
        buttons: true,
        dangerMode: false
    }).then((borrar) => {
        if (borrar) {
            $.ajax({
                type: "POST",
                url: url,
                success: function (data) {
                    if (data.success) {
                        toastr.success(data.message);
                        datatable.ajax.reload();
                    }
                    else {
                        toastr.error(data.message);
                    }
                }
            });
        }
    });

    </script>
}